
{
    static with inherited _ = std Module;

    own document  = (here / 'document' / '_.eco') importIn: self;
    own gfx       = (here / 'gfx' / '_.eco') importIn: self;
    own app       = (here / 'app' / '_.eco') importIn: self;
}
