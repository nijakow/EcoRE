
{
    static with inherited _ = std Module;
    with $battery = $;

    with           own base     = (here / 'base'     / '_.eco') importIn: self;
    with inherited own elements = (here / 'elements' / '_.eco') importIn: self;
}
