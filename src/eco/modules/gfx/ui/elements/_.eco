
{
    static with inherited _ = std Trait;

    with own core     = (here / 'core.eco') importIn: $;
    with own extended = (here / 'extended.eco') importIn: $;
    with window       = (here / 'window.eco') importIn: $;   
}
