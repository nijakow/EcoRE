{
    static with inherited _ = std Clonable;
    with $battery = $package;

    ui => gfx ui;

    own framework = (here / 'framework' / '_.eco') importIn: self;
    own bootstrap = (here / 'bootstrap.eco') importIn: self;
}
