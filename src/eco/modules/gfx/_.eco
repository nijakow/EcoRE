
{
    static with inherited _ = std Module;
    with $battery = $;

    eve => $battery eve;
    own morphic = (here / 'morphic' / '_.eco') importIn: self;
    own ui      = (here / 'ui' / '_.eco') importIn: self;
}
