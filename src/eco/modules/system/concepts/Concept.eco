
{
    static with inherited _ = std Clonable;

    id <- #'concept';

    with inherited trait = {
        static with inherited _ = std Trait;

        initWithId: _id => (
            id <- _id.
          ^ self
        );

        + other => No;  "TODO: Implement combination"
        / other => No;  "TODO: Implement introspection"

        writeOn: w => w << 'the concept ' << id;
    };
}
