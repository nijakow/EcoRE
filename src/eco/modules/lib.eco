{
    static with inherited _ = std Module;

    ecosphere = (here / 'ecosphere' / '_.eco') importIn: self;
    document  = (here / 'document' / '_.eco') importIn: self;
    gfx       = (here / 'gfx' / '_.eco') importIn: self;
}
