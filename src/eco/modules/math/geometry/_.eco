
{
    static with inherited _ = std Module;

    own orientation = (here / 'orientation.eco') importIn: self;

    own point       = (here / 'point.eco') importIn: self;
    own rectangle   = (here / 'rectangle.eco') importIn: self;
}
