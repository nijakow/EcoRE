
{
    static with inherited _ = std Module;
    
    own MorphRepresentationTrait = {
        static with inherited _ = std Trait;

        renderPlacedMorph: morph OnRenderer: renderer => subclassResponsibility;

        writeOn: w => w << 'a morph representation';
    };
}
