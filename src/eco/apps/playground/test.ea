{
    static with inherited _ = std Module;
    with app = $;

    Main => (
        (framework actions open) ui BuildAndOpen: [ :ui |
            ui UndecoratedWindow(ui Connection)
        ].
    );
}
