
{
    static with inherited _ = std Module;
    with app = $;

    own core = (here / 'core' / '_.eco') importIn: self;

    SampleMorph => (
        | morph <- (core MorphInstance) Behavior: (core MorphBehavior New) |

        (morph behavior) layouter: (((core layouting) layouters StackLayouter) New);

      ^ morph
    );
}
