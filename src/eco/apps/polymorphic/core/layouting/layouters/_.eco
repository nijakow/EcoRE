
{
    static with inherited _ = std Module;

    own FlexLayouter  = (here / 'FlexLayouter.eco') importIn: $package;
    own StackLayouter = (here / 'StackLayouter.eco') importIn: $package;

    NewDefault => StackLayouter New;
}
