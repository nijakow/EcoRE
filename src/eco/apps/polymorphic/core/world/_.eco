
{
    static with inherited _ = std Module;

    own Manager = (here / 'Manager.eco') importIn: $package;
    own World   = (here / 'World.eco')   importIn: $package;

    NewWorld => World Manager: (Manager New);
}
