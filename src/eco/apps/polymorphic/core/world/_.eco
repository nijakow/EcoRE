
{
    static with inherited _ = std Module;

    own MorphManager = (here / 'MorphManager.eco') importIn: $package;
    own Controller   = (here / 'Controller.eco')   importIn: $package;
    own World        = (here / 'World.eco')        importIn: $package;

    NewWorld => World Manager: (MorphManager New);
}
