
{
    static with inherited _ = std Module;

    own Paper        = (here / 'Paper.eco')        importIn: $package;
    own PaperProgram = (here / 'PaperProgram.eco') importIn: $package;
    own PlacedMorph  = (here / 'PlacedMorph.eco')  importIn: $package;
}
