
{
    static with inherited _ = std Module;

    own morph  = (here / 'morph'  / '_.eco') importIn: $package;
    own paper  = (here / 'paper'  / '_.eco') importIn: $package;
    own stream = (here / 'stream' / '_.eco') importIn: $package;
}
