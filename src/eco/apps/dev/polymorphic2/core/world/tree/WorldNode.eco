
{
    isa std Classlike;

    Instance :: Clonable
    {
        |
            world
        |

        {
            initWithWorld: w => (
                world <- Yes: w.
              ^ self
            );

            parent => No;

            writeOn: w => w << 'a world node';
        }
    };

    World: world => (Instance clone) initWithWorld: world;
}
