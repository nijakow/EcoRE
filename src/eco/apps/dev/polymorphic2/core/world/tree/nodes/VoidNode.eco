
{
    isa std Singleton;

    parent => No;
    world  => No;

    addChild:    child => self;
    removeChild: child => self;

    childrenDo:        block => self;
    childrenReverseDo: block => self;

    writeOn: w => w << 'a void node';
}
