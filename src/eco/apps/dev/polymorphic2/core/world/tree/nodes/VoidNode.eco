
{
    isa std Singleton;

    with inherited Node :: $package;

    init => (
        $(Node) init.
      ^ self
    );

    parent => No;
    world  => No;

    reparent: p => self;
    disconnect  => self;

    writeOn: w => w << 'a void node';
}
