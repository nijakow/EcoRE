
{
    isa Trait;

    runWithEvent: event OnSuccess: success OnFailure: failure => subclassResponsibility;
    runWithEvent: event OnSuccess: success                    => runWithHead: head Body: body OnSuccess: success OnFailure: [];

    writeOn: w => w << 'an event rule';
}
