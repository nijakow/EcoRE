
{
    isa std Classlike;

    Instance :: std Clonable
    {
        |
            node,
            morphs
        |

        {
            protected polymorphic <- $package;

            init => (
                node   <- (((polymorphic core morph) tree nodes) WorldNode) World: self.
                morphs <- std util newVector.
              ^ self
            );

            addMorph: morph => (
                morphs add: morph. "TODO, FIXME, XXX: Add only if not present!"
              ^ self
            );

            removeMorph: morph => (
                morphs remove: morph.
              ^ self
            );

            iAmDirty: morph => (
                morph redrawInPlace.
              ^ self
            );

            iHaveChanged: morph => (
                morph redrawFreely.
              ^ self
            );

            tickAll => morphs do: [ :each | each tick ];

            writeOn: w => w << 'a polymorphic world';
        }
    };

    New => Instance new;
}
