
std Module
{
    Behavior :: (here / 'Behavior.eco') importIn: $package;
    State    :: (here / 'State.eco')    importIn: $package;
    Kernel   :: (here / 'Kernel.eco')   importIn: $package;
    Morph    :: (here / 'Morph.eco')    importIn: $package;

    tree     :: (here / 'tree'     / '_.eco') importIn: $package;
    behavior :: (here / 'behavior' / '_.eco') importIn: $package;
}
