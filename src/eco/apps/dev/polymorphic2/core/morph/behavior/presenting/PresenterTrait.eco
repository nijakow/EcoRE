
{
    isa std Trait;

    protected polymorphic <- $package;

    minWidthOfKernel:  kernel => subclassResponsibility;
    minHeightOfKernel: kernel => subclassResponsibility;

    drawOn: stream Kernel: kernel => subclassResponsibility;

    writeOn: w => w << 'a presenter';
}
