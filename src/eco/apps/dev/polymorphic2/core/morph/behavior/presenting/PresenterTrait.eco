
{
    isa std Trait;

    protected polymorphic <- $package;

    minWidthOfKernel:  kernel => (kernel drawOnEmptyPaper) realWidth;
    minHeightOfKernel: kernel => (kernel drawOnEmptyPaper) realHeight;
    minBoundsOfKernel: kernel => (kernel drawOnEmptyPaper) realBounds;

    drawOn: stream Kernel: kernel => subclassResponsibility;

    writeOn: w => w << 'a presenter';
}
