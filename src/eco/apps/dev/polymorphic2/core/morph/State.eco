
{
    static with inherited _ = std Classlike;

    own Instance = {
        static with inherited _ = std Clonable;

        seed;

        with inherited trait = {
            static with inherited _ = std Trait;
             
            initWithSeed: _seed => (
                seed <- _seed.
              ^ self
            );
             
            writeOn: w => w << 'a polymorph state';
        };
    };

    Seed: seed => (Instance clone) initWithSeed: (Yes: seed);
    New        => (Instance clone) initWithSeed: No;
}
