
{
    isa std Classlike;

    Instance :: Clonable
    {
        |
            world
        |

        {
            with inherited Node <- $package;

            initWithWorld: w => (
                world <- Yes: w.
              ^ self
            );

            parent => No;

            writeOn: w => w << 'a world node';
        }
    };

    World: world => (Instance clone) initWithWorld: world;
}
