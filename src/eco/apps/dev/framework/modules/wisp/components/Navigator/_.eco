
{
    isa std Classlike;

    Instance :: Clonable
    {
        {
            browseTo: object => (
                (std io out << 'Browsing to ' << object) newline commit.
              ^ self
            );

            writeOn: w => w << 'a navigator';
        }
    };

    New => Instance new;
}
