
std Module
{
    paper   :: (here / 'paper'   / '_.eco') importIn: $package;
    stream  :: (here / 'stream'  / '_.eco') importIn: $package;
    objects :: (here / 'objects' / '_.eco') importIn: $package;
    morph   :: (here / 'morph'   / '_.eco') importIn: $package;
    world   :: (here / 'world'   / '_.eco') importIn: $package;
    event   :: (here / 'event'   / '_.eco') importIn: $package;

    RenderingContext :: (here / 'RenderingContext.eco') importIn: $package;

    NewBehavior => (morph behavior building) Builder new;
}
