
{
    isa Trait;

    accepts?: event                                         => subclassResponsibility;
    forwardEvent: event ToHandler: handler Context: context => subclassResponsibility;

    writeOn: w => w << 'an event predicate';
}
