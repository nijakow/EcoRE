
{
    static with inherited _ = std Classlike;

    own Instance = {
        static with inherited _ = std Clonable;

        protected program <- No;

        with inherited trait = {
            static with inherited _ = std Trait;

            writeOn: w => w << 'a paper program';
        };
    };

    New => Instance new;
}
