"Create a simple object"

| x |

x <- {
    x, y,
    z = {}
},

| y |

y <- {
    a, b, c,
    foo => bar;
    bar(x, y, z) => baz(x, y, self);
    baz(x, y, z) => x;
},

y bar({}, {}, {});

| z |
z <- {
    test_varargs(x, ...) => (
        'ecosphere.ecore.io.print'(x);
        'ecosphere.ecore.io.print'(x);
        'ecosphere.ecore.io.print'(...);
    )
},

^ (
    | the_closure |
    the_closure <- [ 42 ],
    z test_varargs(1, 2, 3),
    'ecosphere.ecore.io.print'('ecosphere.ecore.flow.value'(the_closure))
  )
