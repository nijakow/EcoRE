"Create a simple object"

| x |

x <- {
    x, y,
    z = {}
},

| y |

y <- {
    a, b, c,
    foo => bar;
    bar(x, y, z) => baz(x, y, self);
    baz(x, y, z) => x;
},

y bar({}, {}, {});

^ (
    'ecosphere.ecore.io.print'(6),
    'ecosphere.ecore.io.print'(7),
    'ecosphere.ecore.io.print'(8),
    'ecosphere.ecore.io.print'(9),
    'ecosphere.ecore.io.print'(y),
    'ecosphere.ecore.io.print'(self)
  )
