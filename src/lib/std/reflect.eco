"
    reflect.eco - The `std reflect` package of the Ecosphere
"

#=std/reflect: {
    Mirror = {
        reflectee;

        with inherited trait = {
            init: the_reflectee => (
                reflectee <- the_reflectee.
              ^ self
            );

            slotNames => (
                | type <- #'ecosphere.ecore.reflect.get_type'(reflectee) |
                "TODO: Type slot info"
            );
        };

        with inherited parent = #<:std/Clonable>;
    }
}
