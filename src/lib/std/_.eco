"
    std.eco - The std part of the Ecosphere Standard Library
"

#=std: {
    with globals = #<:globals>;

    DefaultBehavior = #<:std/DefaultBehavior>;
    Clonable = #<:std/Clonable>;

    any = #<:std/bool/false>;

    with bool = #<:std/bool>;
    with maybe = #<:std/maybe>;

    io = #<:std/io>;
    util = #<:std/util>;
    reflect = #<:std/reflect>;
}
