"
    util.eco - The `std util` package of the Ecosphere
"

{
    Pair = {
        first, second;

        with trait = {
            isPair => true;
            first: f second: s => (
                first  <- f.
                second <- s.
              ^ self
            );
        };

        with own parent = #<:std/Object>;
    }
}
