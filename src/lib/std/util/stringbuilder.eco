#=std/util/StringBuilder: {
    chars;

    with inherited trait = {
        init => (
            chars <- std util newVector.
            ^ self
        );

        append: c => (
            chars append: c.
            ^ self
        );

        asString => chars asString;
    };

    with inherited parent = #<:std/Clonable>;
}
