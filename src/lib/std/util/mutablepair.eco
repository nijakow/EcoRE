#=std/util/MutablePair: {
    first, second;

    with inherited trait = {
        initWithFirst: f Second: s => (
            first: f second: s.
            ^ self
        );

        first: f => first <- f;
        second: s => second <- s;

        first: f second: s => (
            first: f. second: s.
        );

        writeOn: w => w << '(' << first << ' cons: ' << second << ')';
    };

    with inherited parent = #<:std/Clonable>;
}
