
#=lang/eco/parser/Parser/CharChecks: {
    IsBin(c) => (
        "TODO: Rework this function!"
        ([
            [
                [ [ c >= #\a ] and: [ c <= #\z ] ] or: [ [ c >= #\A ] and: [ c <= #\Z ] ]
            ] or: [ [ c >= #\0 ] and: [ c <= #\9 ] ]
        ] or: [ c == #\_ ]) not
    );
}
