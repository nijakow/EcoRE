{
    static with inherited _ = std Module;

    optimizer <- (std os files here / '..' / 'modules' / 'ecosphere' / 'optimizer' / '_.eco') import Init;

    f(x) => [ x * x ] value;
    g(x) => 42;
    h(x) => [ (std io out << 'Hi!') newline commit ] repeat;
    i(x) => [ [ [ a ] repeat. [ b ] repeat ] repeat ] value;
    j(x) => [ foo ] value;

    Main => (
        "(optimizer Disassemble: #'j' In: self) dumpOn: (std io out)."
        optimizer RewriteAST: (optimizer BuildAST: (optimizer Disassemble: #'h' In: self)).
    );
} Main.
