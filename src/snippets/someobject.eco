{
    static with inherited _ = std Clonable;

    x <- 0,
    y <- 0;

    with inherited trait = {
        static with inherited _ = std Trait;

        init => (
          ^ self
        );

        x: xv => x <- xv;
        y: yv => y <- yv;

        x: xv y: yv => x: xv ~ y: yv;
    };
}
