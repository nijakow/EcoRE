{
    static with inherited _ = std Module;

    optimizer = $;

    RegisterEliminator = (here / 'RegisterEliminator.eco') importIn: self;

    Rewrite: ast Context: context => (
        RegisterEliminator clone init: context
          ~ process: ast
    );
}
