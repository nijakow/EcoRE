
    {
        static with inherited _ = std Clonable;

        r, g, b;

        with inherited trait = {
            initWithR: _r G: _g B: _b => (
                r <- _r.
                g <- _g.
                b <- _b.
              ^ self
            );

            writeOn: w => w << 'a color';
        };
    };
