
{
    static with inherited _ = std Module;
    morphic = (here / '..' / 'morphic' / '_.eco') import;

    ui => self;

    with builder = (here / 'builder' / '_.eco') importIn: self;

    MorphicApp = (here / 'morphicapp.eco') importIn: self;

    App = (here / 'testapp.eco') importIn: self;

    Main => morphic RunWithWidth: 1400 Height: 800 App: App new;
}
