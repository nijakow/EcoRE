
{
    static with inherited _ = std Module;
    with $battery = $;

    builder = (here / 'builder' / '_.eco') importIn: self;

    App = (here / 'App.eco') importIn: self;

    Main => (
        morphic RunWithWidth: 1400 Height: 800 App: App new;
    );
}
